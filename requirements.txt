# Runtime dependencies for the Streamlit app (app/streamlit_app.py).
# Pinned to the versions verified locally. Streamlit Community Cloud installs
# from this file. (scikit-learn / scipy are deliberately NOT required — see CLAUDE.md.)
streamlit==1.58.0
streamlit-folium==0.27.2
folium==0.20.0
osmnx==2.1.0
networkx==3.6.1
geopandas==1.1.3
shapely==2.1.2
pandas==3.0.3
numpy==2.4.6
requests==2.34.2
